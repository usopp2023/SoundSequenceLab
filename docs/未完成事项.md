# 未完成事项 / TODO（按落地文档 V3 §11 等）

记录已知但本轮未做完的事，便于后续接力。更新日期：2026-06-03。

## 🔒 被「正式 AppID」卡住（拿到 AppID + secret 才能做）

### #3 真实 openid / 账号体系
- **现状**：用测试号 `touristappid`，`wx.login` 在游客模式只给模拟 code，后端 `/api/login` 返回**伪 openid**（`dev_<sha1>`）。
- **影响**：同一用户换设备 / 真账号后，档案、收藏、解锁状态**不会跟随**（因为 openid 不是真的）。
- **要做**：
  1. 注册正式小程序 AppID，拿到 AppID + secret。
  2. 后端 `server/app/api/routes_auth.py` 的 `login` 接 `code2session`（代码里已留 TODO）：
     `GET https://api.weixin.qq.com/sns/jscode2session?appid=&secret=&js_code=&grant_type=authorization_code` → 真实 openid / session_key。
  3. `project.config.json` / 开发者工具里把 appid 换成正式 AppID。
- **登录头像昵称**目前存在本地 `storage`（`store.profile`）。拿到真账号后，应在 `pages/login/login.js` 的 `doLogin()` 里把昵称/头像**同步到后端账号**（按 openid 持久化），并在头像上传到后端静态目录（现在只存了微信返回的临时文件路径，可能过期）。

### 线下认领的「微信冷扫码直达」（小程序码）—— 仅这一步要 AppID
- **现状**：认领已支持**扫码**——现场屏幕出二维码（编码认领码），用户在小程序「兑换码」页点「扫码认领」用 `wx.scanCode` 一扫即认领（**不需要 AppID**，已做）；也可手输 8 位码。
- **仍要做（要 AppID）**：把现场二维码换成微信**小程序码**（`wxacode.getUnlimited`，需 access_token），这样用户**用微信原生扫一扫就能冷启动打开小程序并自动认领**，连"先打开小程序再扫"都省了。后端 `GET /api/kiosk/claim-qr` 现在返回普通二维码 SVG，到时改成返回小程序码即可，前端不用动。

## ✅ 已做：线下认领闭环（公用体验机 → 本人认领）
- **现场体验机**：`kiosk/index.html`（H5，全屏，复用原型设计），接同一套后端 `/api/upload`、`/api/generate`、`/api/jobs`、`/api/kiosk/claim-code`。**答题流程与小程序一致**（语音优先 MediaRecorder→`/api/upload`，打字兜底，上一题、答案恢复、空输入禁用下一题）→生成→结果→出**二维码 + 8 位认领码**→「完成，下一位」重置。可用 `?api=<后端地址>` 指定后端。浏览器录音需 https/localhost，否则自动降级打字。
- **后端**：`POST /api/kiosk/claim-code` 出码（带 `qrUrl`）；`GET /api/kiosk/claim-qr?code=` 返回二维码 SVG；`POST /api/redeem` 扩展为同时处理演示码（仅解锁权益）与认领码（绑定结果到本人 openid + 解锁权益，一次性）。`ClaimCode` 表持久化。
- **小程序**：兑换码页可**扫码认领**（`wx.scanCode`）或手输码 → 认领码把作品绑到本人并跳到结果页（`mode=claimed`），演示码仍只解锁权益。
- 仍待真实化：只剩「微信冷扫码直达」（小程序码，要 AppID，见上）。现场设备身份用 `X-Openid: kiosk` 占位，正式可换成设备级标识。

## 🧭 产品决策已定，但仍待真实化

### #11 登录授权（静默登录 + 按需头像昵称）
- ✅ 已做：启动 `app.js` **静默** `wx.login` 拿 openid，直接能玩、不挡路。头像昵称做成**按需**：在 me / 我的活动点身份区进 `pages/login/login`，用微信最新「头像昵称填写能力」(`open-type="chooseAvatar"` + `type="nickname"`) 采集，存 `store.profile`，me / act-mine 显示。未设置时显示「微信用户 + 默认头像」并提示「点这里设置头像昵称」。
- 说明：微信旧的 `wx.getUserProfile` 授权弹窗已废弃（2022 起拿不到真实昵称头像），所以不存在"一键授权拿头像昵称"，只能让用户自己填。
- ⏳ 待真实化：见上面 #3（真 openid + 后端账号 + 头像上传持久化）。

### #12 真实通知事件
- **现状**：`pages/notif` 是 3 条写死数据。
- **要做**：「你的曲子已生成」「有人和你很像」应由**真实事件**触发（生成完成回调 / 匹配到相似用户），点进去的目标页数据也要是真的。需要后端事件/消息表 + 订阅消息（`subscribeMessage`）。

## 🎵 核心能力（已接真实，按环境变量门控）

- ✅ **情绪分析**：`StepFunAnalysis`（阶跃星辰 OpenAI 兼容）一次结构化调用 → 9 桶情绪本体（`buckets.py`）+ 人格/四维/报告。配 `ANALYSIS_API_KEY` 即启用，否则回退占位。实测约 20s（可 `ANALYSIS_MODEL=step-1-8k` 提速）。
- ✅ **音乐生成（已跑通真 Suno）**：`SunoMusic` 经自建 suno-api 用**有头浏览器生成**（`/api/ui_generate` 把 9 桶 tags 输进 suno.com → Create → 出曲）→ 轮询 `/api/get` 取回 → 代理下载转存 `static/generated/`，失败回退兜底曲，结果页"生成中→点亮"。2026-06-06 端到端实测通过（约 2-3min/首）。
  - 完整搭建/排障/局限见 **`docs/Suno接入说明.md`**（含致命坑：localhost 被系统代理劫持→`trust_env=False`；无头被 hCaptcha 拦→有头模式；选择器过时→重抓 DOM）。
  - **局限**：有头浏览器（每次弹窗、需挂 Clash、机器要有屏幕）、cookie 会过期、Suno 改版可能要重调选择器。适合 dev/小规模演示。
  - **真民乐天花板**仍是"国风近似"；要"真民乐技法"后续上 ACE-Step + LoRA（见 `音乐生成方案选型.md`），换适配器即可、前端不动。
  - suno-api 是第三方服务，已 gitignore；本机 cookie/proxy/补丁都在 `suno-api/`（不上传）。
- ⏳ **9 桶映射表待校准**：`buckets.py` 的调式/BPM/乐器/技法为初版，需演奏者过一遍。
- ⏳ **asr.py 仍占位**：现按文本（识别文字/打字）分析；要"听音频"可接阶跃音频模型或专门 ASR。
- ⏳ 真实推送通知「你的曲子已生成」需 `subscribeMessage`（要正式 AppID）；现为结果页轮询点亮。

## 🌐 上线相关

- **ICP 备案 + https 生产域名**：后端要部署到备案好的公网域名，填进小程序后台「服务器域名」白名单；`miniprogram/config/index.js` 的 `API_BASE` 切到 prod，关掉开发者工具「不校验合法域名」。备案最慢，趁早办。
- **分享卡专属皮肤**：解锁后"专属分享卡皮肤"目前只是奖励**预览**，未真正更换 `onShareAppMessage` 的卡面图。
